# הנחיות לפרויקט

## כתיבה

**לעולם לא להשתמש בקו מפריד ארוך.** לא em dash (U+2014) ולא en dash (U+2013),
בשום מקום: לא בטקסט שהאוהד רואה, לא ב־README, לא בהערות בקוד, לא בהודעות
קומיט ולא בתיאורי PR. במקומם: פסיק, נקודתיים, נקודה, סוגריים, או מקף רגיל
(מינוס רגיל) כשמדובר בטווח כמו 2016-2019.

שני יוצאי דופן, ושניהם תווים שהם **נתון** ולא פיסוק:

1. הביטוי הרגולרי `NUM_RUN` ב־`app/js/app.js` מזהה מפרידים שמגיעים ממקורות
   חיצוניים, וביניהם en dash, ולכן הוא חייב להכיר אותו.
2. מקף עברי (U+05BE) הוא לא קו מפריד ארוך והוא תקין: כך נכתב ״ו־ONE״.

הבדיקה: `grep -rnP "\x{2014}|\x{2013}" .` צריך להחזיר רק את השורה של
`NUM_RUN` ואת הקובץ הזה.

## עברית

העברית באפליקציה צריכה להישמע כמו עברית של אוהד, לא כמו תרגום. ניסוח שלא
מובן בקריאה ראשונה הוא באג לכל דבר. שימו לב במיוחד להחלפת קו מפריד בפסיק:
לפעמים היא יוצרת שתי פסוקיות שנדחסו למשפט אחד, ואז הנקודה או הנקודתיים הן
התשובה הנכונה.

מספרים בתוך טקסט עברי: תוצאה כמו 92-89 מתהפכת על המסך בגלל אלגוריתם ה־bidi,
ולכן כל טקסט מהנתונים עובר דרך `prose()`/`proseInto()` שעוטפים רצפי מספרים
ב־`<bdi>`. טקסט חדש שמכיל תוצאות חייב לעבור שם גם הוא.

## נתונים

אין להמציא נתונים על אנשים אמיתיים. מה שלא אומת, לא נכתב: כותבים את ההישג
במילים במקום להמציא לו מספר.
